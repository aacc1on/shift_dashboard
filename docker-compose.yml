version: '3.8'
services:
  soc-dashboard:
    build: .
    ports:
      - "3000:3000"
    environment:
      - SOC_USER=admin
      - SOC_PASS=soc2026
      - SESSION_SECRET=changeme
      - PORT=3000
    volumes:
      - ./data:/app/data
      - ./audit.log:/app/audit.log
    restart: unless-stopped
